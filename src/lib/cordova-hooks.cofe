# window.xParseURLToPath = (url) ->
#   link = document.createElement('a')
#   link.href = url
#   link.pathname

window.handleOpenURL = (url) ->
  console.log "Trying to open URL: #{url}"
  # path = xParseURLToPath(url)
  # app.router.open(path)
